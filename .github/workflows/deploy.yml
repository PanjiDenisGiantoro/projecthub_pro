name: CI/CD ProjectHub Pro

on:
  push:
    branches: [ dev, master ]
  pull_request:
    branches: [ master ]

jobs:
  # ─────────────────────────────────────────────────────────
  # JOB 1: CI Check — jalan saat push ke dev atau PR ke master
  # ─────────────────────────────────────────────────────────
  ci:
    name: CI Check
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev' || github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, bcmath, pdo_mysql, tokenizer, xml, ctype, json
          coverage: none

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Copy .env
        run: cp .env.example .env

      - name: Generate app key
        run: php artisan key:generate

      - name: Check PHP syntax
        run: find app/ -name "*.php" | xargs php -l

      - name: Validate composer.json
        run: composer validate

  # ─────────────────────────────────────────────────────────
  # JOB 2: Deploy Dev — jalan saat push ke dev
  # ─────────────────────────────────────────────────────────
  deploy-dev:
    name: Deploy ke Dev
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'
    needs: ci

    steps:
      - name: Deploy Dev via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 60s
          command_timeout: 15m
          script: |
            set -e

            echo ">>> Masuk ke direktori dev"
            cd /root/projecthub_pro_dev

            echo ">>> Pull kode terbaru dari dev"
            git pull origin dev

            echo ">>> Install Composer dependencies"
            COMPOSER_ALLOW_SUPERUSER=1 composer install --optimize-autoloader --no-interaction --ignore-platform-req=php

            echo ">>> Install NPM dan build assets"
            npm ci --no-audit --no-fund
            npm run build

            echo ">>> Jalankan migrasi database dev"
            php artisan migrate --force

            echo ">>> Build cache"
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            php artisan event:cache

            echo ">>> Restart queue worker"
            php artisan queue:restart

            echo ">>> Deploy DEV selesai! → projecthub.arunikasolusiinovasi.tech"

  # ─────────────────────────────────────────────────────────
  # JOB 3: Deploy — jalan saat push ke master (PR di-merge)
  # ─────────────────────────────────────────────────────────
  deploy:
    name: Deploy ke Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 60s
          command_timeout: 15m
          script: |
            set -e

            echo ">>> Masuk ke direktori project"
            cd /root/projecthub_pro

            echo ">>> Pull kode terbaru dari master"
            git pull origin master

            echo ">>> Install Composer dependencies (production)"
            COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-req=php

            echo ">>> Install NPM dan build assets"
            npm ci --no-audit --no-fund
            npm run build

            echo ">>> Jalankan migrasi database"
            php artisan migrate --force

            echo ">>> Build cache"
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            php artisan event:cache

            echo ">>> Restart queue worker"
            php artisan queue:restart

            echo ">>> Restart PHP-FPM"
            systemctl restart php8.4-fpm || service php8.4-fpm restart || true

            echo ">>> Deploy PRODUCTION selesai! → projecthub.arunikasolusiinovasi.tech"
